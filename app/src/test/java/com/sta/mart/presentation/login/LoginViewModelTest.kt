package com.sta.mart.presentation.login

import kotlinx.coroutines.ExperimentalCoroutinesApi
import kotlinx.coroutines.test.runTest
import org.junit.Test

@ExperimentalCoroutinesApi
class LoginViewModelTest {

    @Test
    fun `initial state should be Idle`() = runTest {
    }

    @Test
    fun `press login should show loading`() = runTest {
    }

    @Test
    fun `account and secret are right should response success state`() = runTest {
    }

    @Test
    fun `account and secret are wrong should response error state`() = runTest {
    }
}